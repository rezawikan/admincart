export default {
  SET_DATA_RETURN(state, returns) {
    state.returns = returns
  },

  SET_META(state, meta) {
    state.meta = meta
  },

  SET_DATA_CURRENT_RETURN(state, current_return) {
    state.current_return = current_return
  },
}
