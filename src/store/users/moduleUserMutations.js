export default {
  SET_DATA_USERS(state, users) {
    state.users = users
  },

  SET_DATA_META(state, meta) {
    state.meta = meta
  },

  SET_DATA_CUSTOMER(state, customer) {
    state.customer = customer
  },
}
