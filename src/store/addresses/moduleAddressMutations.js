export default {
  SET_DATA_ADDRESSES(state, addresses) {
    state.addresses = addresses
  },

  SET_DATA_PUBLIC_ADDRESSES(state, publicAddresses) {
    state.publicAddresses = publicAddresses
  },

  SET_DATA_CITIES(state, cities) {
    state.cities = cities
  },

  SET_DATA_PROVINCES(state, provinces) {
    state.provinces = provinces
  },

  SET_DATA_SUBDISTRICT(state, subdistricts) {
    state.subdistricts = subdistricts
  }

}
