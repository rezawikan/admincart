export default {
  SET_DATA_ORDER(state, orders) {
    state.orders = orders
  },

  SET_META(state, meta) {
    state.meta = meta
  },

  SET_DETAIL(state, detail) {
    state.detail = detail
  },
}
