export default {
  SET_PRODUCTS(state, products) {
    state.products = products
  },

  SET_META(state, meta) {
    state.meta = meta
  },

  SET_PRODUCT_TYPE(state, producttype) {
    state.producttype = producttype
  },

  SET_PRODUCT_VARIATIONS(state, variations) {
    state.variations = variations
  },

  SET_META_PRODUCT_VARIATIONS(state, metavariations) {
    state.metavariations = metavariations
  }
}
